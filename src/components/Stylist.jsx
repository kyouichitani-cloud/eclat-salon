import { stylists } from '../data/stylists'
export default function Stylist({onReserve}){return <section className="stylist section-theme" id="stylist" data-bg="#111111"><div className="stylist__intro"><p className="eyebrow">03 — ARTISTS</p><h2>STYLIST</h2><p>技術の先にある、その人だけの美しさを見つける。</p></div><div className="stylist-track">{stylists.map(s=><article className="stylist-card" key={s.id}><div className="stylist-card__image"><img src={s.image} alt={`${s.id} ${s.role}`} width="900" height="1125" loading="lazy" decoding="async"/><span>{s.no}</span></div><div className="stylist-card__body"><p>{s.role}</p><h3>{s.id}</h3><ul>{s.skills.map(x=><li key={x}>{x}</li>)}</ul><p>{s.bio}</p><button onClick={()=>onReserve(s.id)}>指名して予約 <span>→</span></button></div></article>)}</div></section>}
