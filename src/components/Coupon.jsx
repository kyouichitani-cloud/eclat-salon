import { AnimatePresence, motion } from 'framer-motion'
import { useState } from 'react'
import { couponCategories, coupons } from '../data/coupons'
import MagneticButton from './MagneticButton'
export default function Coupon({onReserve}){const[open,setOpen]=useState(false),[tab,setTab]=useState(couponCategories[0]);const list=coupons.filter(c=>c.category===tab);return <section className={`coupon section-theme ${open?'coupon--open':''}`} id="coupon" data-bg="#d8d0c4"><div className="coupon__hero"><p className="eyebrow">05 — PRIVATE OFFER</p><h2>COUPON</h2><p>あなたに合う、特別なプランを。</p>{!open&&<MagneticButton className="button button--dark" onClick={()=>setOpen(true)}>DISCOVER COUPONS</MagneticButton>}</div><AnimatePresence>{open&&<motion.div className="coupon__content" initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} transition={{duration:.65}}><div className="coupon-tabs">{couponCategories.map(c=><button className={tab===c?'active':''} key={c} onClick={()=>setTab(c)}>{c}</button>)}</div><AnimatePresence mode="wait"><motion.div className="coupon-grid" key={tab} initial={{opacity:0,y:24}} animate={{opacity:1,y:0}} exit={{opacity:0,y:20}}>{list.map(c=><article key={c.id}><p>{c.category}</p><h3>{c.title}</h3><div><del>{c.regular}</del><strong>{c.price}</strong></div><small>{c.condition}</small><button onClick={()=>onReserve(c)}>このクーポンで予約 <span>→</span></button></article>)}</motion.div></AnimatePresence></motion.div>}</AnimatePresence></section>}
